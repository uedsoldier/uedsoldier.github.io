{
  "name": "uedsoldier.github.io",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm run build && live-server --open=./dist --port=5500",
    "build": "sh ./scripts/build.sh",
    "test": "echo \"No tests configured\" && exit 0"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "dependencies": {
    
  }
}
